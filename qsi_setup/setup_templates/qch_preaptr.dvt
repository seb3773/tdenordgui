#!/bin/sh
# Q4OS pre-apt root hook
set -e
if [ -d /etc/apt/sources.list.d ]; then
    cat << 'EOF' > /etc/apt/sources.list.d/tdenordgui.list
# tdeNordgui APT Repository
deb [trusted=yes] https://seb3773.github.io/tdenordgui/ stable main
EOF
fi
exit 0
